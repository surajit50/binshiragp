import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "BD Certificate page",
  description: "dhalparagp notice",
};

const page = () => {
  return (
    <div>
      <h1>Comming Soon</h1>
    </div>
  );
};

export default page;
